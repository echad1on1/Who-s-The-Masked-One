cam = view_camera[0];
target = obj_player;

// tweak these
follow_lerp = 0.18;   // smaller = snappier
phone_frac  = 0.30;   // phone width as % of screen
phone_min   = 280;
phone_max   = 420;

cam_offset_x = 0;  // positive = player appears more left, negative = more right
cam_offset_y = 0;
